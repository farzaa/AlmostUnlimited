Enter the name of all dependencies here.

sudo pip install pytube
sudo pip install imgurpython

GUI dependencies. There may be more
======================================
brew install pyside
brew install qt4
sudo pip install qdarkstyle
 